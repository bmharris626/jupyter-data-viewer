# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Node
node_modules/
npm-debug.log*
package-lock.json

# Yarn Berry — don't commit cache/state for a public repo
.yarn/cache/
.yarn/install-state.gz

# TypeScript compiled output (intermediate; labextension/ bundle is what ships)
lib/
*.tsbuildinfo

# Top-level labextension build dir (not the one bundled inside the Python package)
/labextension/

# Testing
coverage/
.pytest_cache/
htmlcov/

# Environment
.env
*.local
